//
//  {{ module_info.description }}
//  Created by {{ developer.name }} on {{ date }}.
//

import UIKit

protocol {{ prefix }}{{ module_info.name }}DisplayLogic: AnyObject {
}

final class {{ prefix }}{{ module_info.name }}ViewController: UIViewController {
    private let interactor: {{ prefix }}{{ module_info.name }}BusinessLogic
    private let router: {{ prefix }}{{ module_info.name }}RouterProtocol 

    init(interactor: {{ prefix }}{{ module_info.name }}BusinessLogic, router: {{ prefix }}{{ module_info.name }}RouterProtocol) {
        self.interactor = interactor
	self.router = router
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    // MARK: View lifecycle
    override func loadView() {
        let view = {{ prefix }}{{ module_info.name }}View(frame: UIScreen.main.bounds)
        self.view = view
    }

    override func viewDidLoad() {
        super.viewDidLoad()
    }
}

extension {{ prefix }}{{ module_info.name }}ViewController: {{ prefix }}{{ module_info.name }}DisplayLogic {
}
