//
//  Created by {{ developer.name }} on {{ date }}.
//

protocol {{ prefix }}{{ module_info.name }}ProviderProtocol {
}

final class {{ prefix }}{{ module_info.name }}Provider: {{ prefix }}{{ module_info.name }}ProviderProtocol {

    required init() { }
}
